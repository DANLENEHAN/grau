alembic==1.11.1
configparser==5.3.0
# Using Flask==2.0.1 instead of latest because flask_scoped_session
# is not compatible with Werkzeug>=2.0.3
Flask==2.0.1
Flask-Cors==3.0.10
Flask-SQLAlchemy-Session==1.1
freezegun==1.2.2
# These are mostly the same as the grau app requirements
# in the pyproject.toml file, dependencies section. Bar e.g. psycopg2
psycopg2==2.9.6
pylint==2.17.4
PyYAML==6.0
Werkzeug==2.0.3
